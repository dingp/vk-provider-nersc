apiVersion: v1
kind: Pod
metadata:
  name: simple-test
  annotations:
    nersc.slurm/account: "m1234"
    nersc.sf/tokenSecretName: "sf-api-token"
spec:
  nodeSelector:
    kubernetes.io/hostname: perlmutter-vk
  containers:
  - name: hello
    image: registry.example.com/hello:latest
    command: ["echo"]
    args: ["Hello from Perlmutter via Virtual Kubelet!"]
    resources:
      requests:
        cpu: "1"
        memory: "1Gi"
